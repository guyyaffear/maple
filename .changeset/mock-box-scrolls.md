---
"@maple-kit/ui": patch
---

The mock box scrolls its calls and the "Shown as" and "Flags" panel together,
between the field and the footer, so a short window no longer squeezes the
calls to a sliver.
