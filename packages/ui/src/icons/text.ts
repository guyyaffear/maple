/** Pick a passage: a serif I-beam, which is what a text cursor already is. */
import { createIcon } from "./icon.js";

export const TextIcon = /** @__PURE__ */ createIcon({
  d: "M3 3.5h10 M8 3.5v9 M6 12.5h4",
});
